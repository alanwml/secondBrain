# Checkpoint: pre-context-disambiguation

Created: 2026-09-08

This snapshot preserves the local AI-analysis feature before context ambiguity handling was implemented. It includes the working local server, background analysis queue, AI confirmation flow, note modal, source links, image previews, token usage, and cost metadata.

To restore this checkpoint manually, copy the files in this directory back to the project root. The snapshot does not include runtime data in `data/` or secrets in `.env`.
